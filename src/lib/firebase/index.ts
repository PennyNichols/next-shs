import { app, auth, db, storage, analytics, functions } from './firebase';
// Note: Firebase Admin SDK is only for server-side use and should not be imported in client components

export {
  app,
  auth,
  db,
  storage,
  analytics,
  functions
}