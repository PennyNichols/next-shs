import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
    container: {
        textAlign: 'center',
        padding: '20px',
    },
    heading: {
        color: '#333',
        fontSize: '2em',
    },
    paragraph: {
        color: '#666',
        fontSize: '1.2em',
    },
});

export default useStyles;